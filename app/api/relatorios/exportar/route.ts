import { obterRelatorio } from '@/lib/operacao/relatorios';
export async function GET(req:Request){const p=Object.fromEntries(new URL(req.url).searchParams);const r=await obterRelatorio(p);const d=r.dados;const linhas=[['Equipe','Respostas elegíveis','Dentro da meta','Mediana segundos úteis'],...d.equipes.map(e=>[e.nome||'Sem equipe',e.elegiveis,e.dentro_meta,e.mediana??'']),['Total',d.elegiveis,d.dentro_meta,d.mediana??''],['Conversas iniciadas',d.iniciadas],['Conversas concluídas',d.concluidas],['Respostas excluídas',d.excluidas],['Esperas abertas agora',d.esperas_abertas],['Esperas vencidas agora',d.esperas_vencidas],['Instrumentação desde',d.cobertura||'Não disponível'],['Período',r.inicio,r.final,r.fuso]];const csv=linhas.map(l=>l.map(v=>'"'+String(v).replace(/^[=+@-]/,"'$&").replace(/"/g,'""')+'"').join(';')).join('\r\n');return new Response('\uFEFF'+csv,{headers:{'Content-Type':'text/csv; charset=utf-8','Content-Disposition':'attachment; filename="relatorio-atendimento.csv"','Cache-Control':'no-store'}});}
